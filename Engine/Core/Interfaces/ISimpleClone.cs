namespace Engine;

/// <summary>
/// La interfaz <c>ISimpleClone</c> representa a objetos que pueden ser clonados de forma sencilla
/// por lo que todos responden a la implementación de un único método Clone (Que está como método extensor de la clase).
/// </summary>
public interface ISimpleClone<T> { }
